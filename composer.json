{
    "name": "isolate/isolate",
    "description": "Isolate is an abstraction layer for data persistence that introduce contexts and transactions.",
    "keywords": ["isolate", "orm", "framework", "storage", "data", "odm"],
    "require": {
        "php": ">=5.4.0"
    },
    "require-dev": {
        "phpspec/phpspec": "2.1.*"
    },
    "autoload": {
        "psr-0": {
            "Isolate": "src/"
        }
    },
    "extra": {
        "branch-alias": {
            "dev-master": "1.0-dev"
        }
    },
    "config": {
        "bin-dir": "bin"
    },
    "license": "MIT",
    "authors": [
        {
            "name": "Norbert Orzechowicz",
            "email": "norbert@orzechowicz.pl"
        }
    ],
    "suggest": {
        "isolate/framework": "Framework integrate Isolate components like Unit of Work or Lazy Objects with Isolate abstraction layer."
    }
}
